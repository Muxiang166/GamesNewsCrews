你的思路**很对**，而且是更工程化、更适合求职展示的路线：

> **先 SQL 事件库 / 资讯库 → 再确定性去重和事件聚合 → 再预留向量库/RAG → 再从搜索层开始智能化 → 最后接 LangSmith/LangGraph。**

这比“我先接 LangGraph + LLM + LangSmith，然后再想数据怎么存”靠谱很多。

但我建议你稍微调整一句：
**不要只是“顺着时间找”，而是“以时间为主轴，以事件为单位组织”。**

资讯 Agent 的核心不是存 2000 条新闻，而是从 2000 条新闻里判断：

```text
哪些是同一事件？
哪些是旧闻转载？
哪些是旧事件更新？
哪些是新事件？
哪些只是同游戏不同事件？
```

---

## 你的当前路线可以定成这样

```text
阶段 1：SQL 资讯库
存 raw news items，保证每条资讯可追溯。

阶段 2：确定性去重 / 聚类
用 URL、标题归一化、hash、SimHash、时间窗口、游戏实体做重复判断。

阶段 3：事件库
把资讯条目聚合成 event，而不是只存 news item。

阶段 4：本地 trace / 参数日志
先不接 LangSmith，但把每一步输入输出、分数、阈值、决策记录下来。

阶段 5：搜索智能化
从 query compression / query expansion 开始接 LLM，先 shadow mode。

阶段 6：向量库 / RAG
SQL 作为事实源，向量库作为语义召回索引。

阶段 7：LangSmith / LangGraph
LLM 节点变多、需要追踪和评估时再接。
```

LangSmith 官方定位是 LLM/agent 的 observability、tracing、evaluation 和 monitoring，所以你现在还没有 LLM 调用时，先做本地 trace 表完全合理；等有 LLM 节点后，再接 LangSmith看每一步输入输出、错误点、性能和评估结果更有价值。([LangChain 文档][1])

---

## 最关键的设计：SQL 是主库，向量库是索引

你以后接向量库/RAG时，不要让向量库变成“事实源”。

应该是：

```text
SQL/Postgres/SQLite:
  存真实资讯、事件、来源、时间、URL、判断结果

Vector DB:
  存 title/content/event summary 的 embedding
  用于语义召回相似旧事件

RAG:
  从 SQL + Vector DB 找证据
  给 LLM 做判断/摘要
```

也就是：

```text
SQL = source of truth
Vector DB = semantic search index
LLM/RAG = reasoning and summarization layer
```

这样以后 LLM 判断错了，你还能回到 SQL 查证据，不会变成一团糊。

---

## 你现在应该优先设计这几张表

### 1. `news_items`：原始资讯表

存每条抓到的资讯。

```sql
CREATE TABLE news_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    platform TEXT NOT NULL,
    source_name TEXT,
    url TEXT,
    canonical_url TEXT,
    title TEXT NOT NULL,
    title_norm TEXT,
    snippet TEXT,
    content TEXT,
    author TEXT,
    publish_time DATETIME,
    crawl_time DATETIME NOT NULL,
    first_seen_time DATETIME NOT NULL,
    last_seen_time DATETIME NOT NULL,

    content_hash TEXT,
    title_hash TEXT,
    simhash TEXT,

    raw_json TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

这里最重要的是：

```text
publish_time：网站声称的发布时间
crawl_time：你抓到它的时间
first_seen_time：你的系统第一次看到它的时间
last_seen_time：最后一次看到它的时间
```

因为你之前提到“有些网站 48 小时后才上传旧信息”，这时 **publish_time 和 first_seen_time 的差异** 很关键。

---

### 2. `games`：游戏实体表

```sql
CREATE TABLE games (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    canonical_name TEXT NOT NULL,
    aliases TEXT,
    developer TEXT,
    publisher TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

例如：

```json
{
  "canonical_name": "绝区零",
  "aliases": ["ZZZ", "Zenless Zone Zero", "ゼンレスゾーンゼロ"]
}
```

这一步会直接影响你的搜索召回和同游戏判断。

---

### 3. `news_game_links`：资讯和游戏的关系

一条新闻可能提到多个游戏，所以不要把 `game_id` 直接硬塞进 `news_items`。

```sql
CREATE TABLE news_game_links (
    news_id INTEGER,
    game_id INTEGER,
    confidence REAL,
    match_method TEXT,
    PRIMARY KEY (news_id, game_id)
);
```

`match_method` 可以是：

```text
alias_exact
keyword_match
embedding_match
llm_judge
manual
```

现在先用确定性方法，以后再加 LLM。

---

### 4. `events`：事件表

这是你项目从“资讯搜索器”变成“资讯 Agent”的关键。

```sql
CREATE TABLE events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    game_id INTEGER,
    event_title TEXT NOT NULL,
    event_type TEXT,
    event_status TEXT,

    first_seen_time DATETIME NOT NULL,
    latest_seen_time DATETIME NOT NULL,
    event_time DATETIME,

    summary TEXT,
    confidence REAL,
    source_count INTEGER DEFAULT 0,

    event_fingerprint TEXT,
    embedding_ref TEXT,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

`event_status` 建议先定成：

```text
new_event
event_update
duplicate
stale_repost
same_game_different_event
irrelevant
uncertain
```

这套标签以后也能直接变成你的评测集标签。

---

### 5. `event_news_links`：资讯和事件的关系

```sql
CREATE TABLE event_news_links (
    event_id INTEGER,
    news_id INTEGER,
    relation_type TEXT,
    relevance_score REAL,
    freshness_score REAL,
    same_event_score REAL,
    duplicate_score REAL,
    judge_method TEXT,
    reason TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (event_id, news_id)
);
```

`relation_type` 可以是：

```text
same_event
event_update
duplicate_source
stale_repost
evidence
conflict
irrelevant
```

这张表非常重要。
它能解释为什么某条新闻被归到某个事件里。

---

### 6. `search_runs`：搜索记录表

为以后 LangSmith / tracing 做准备。

```sql
CREATE TABLE search_runs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    run_id TEXT NOT NULL,
    mode TEXT,
    seed_game TEXT,
    seed_query TEXT,
    expanded_queries TEXT,
    platforms TEXT,
    time_window_start DATETIME,
    time_window_end DATETIME,
    parameters_json TEXT,
    started_at DATETIME,
    ended_at DATETIME,
    result_count INTEGER,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

`mode` 可以是：

```text
baseline
shadow
hybrid
llm
```

这会让你以后能很清楚地比较：

```text
不用 LLM 的结果怎样？
LLM 旁路判断怎样？
混合模式有没有提升？
```

---

### 7. `decision_traces`：本地追踪表

你现在不接 LangSmith，但可以先自己记录 trace。

```sql
CREATE TABLE decision_traces (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    run_id TEXT NOT NULL,
    step_name TEXT NOT NULL,
    input_json TEXT,
    output_json TEXT,
    parameters_json TEXT,
    score_json TEXT,
    decision TEXT,
    reason TEXT,
    error TEXT,
    started_at DATETIME,
    ended_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

以后接 LangSmith 时，这些字段很容易映射过去。LangSmith/LangGraph 文档里对 tracing 的描述也是记录应用从输入到输出经历的步骤，每一步可以表示为 run，用于调试、评估和监控。([LangChain 文档][2])

---

## 你的“重复新闻判断”应该分三层

不要只做一个 duplicate。

建议分成：

### 第一层：完全重复

```text
URL 相同
canonical_url 相同
title_norm 相同
content_hash 相同
```

这个确定性判断即可。

---

### 第二层：转载 / 洗稿

```text
标题略有变化
核心事实相同
发布时间滞后
来源不同
没有新增信息
```

这个用：

```text
title similarity
SimHash
关键词重叠
时间差
同游戏判断
```

先做 baseline。

---

### 第三层：旧事件新进展

这是最有价值也最难的。

```text
和旧事件相关
但新增了事实
例如新增直播时间、角色名、上线日期、奖励、官方确认
```

这一步先用规则做：

```text
是否出现新增实体
是否出现新时间
是否出现新数字
是否出现官方源
是否和历史 summary 不同
```

以后再交给 LLM 灰区判别。

---

## 你从搜索开始智能化，非常合理

我赞成你**先从搜索开始接智能化**，而不是先让 LLM 判断新闻真假。

推荐顺序是：

```text
1. Query Compression
2. Query Expansion
3. Search Result Relevance Classifier
4. Gray-zone Same-event Judge
5. Freshness / Update Judge
6. News Card Generator
```

其中第一步风险最低。

比如输入：

```text
《明日方舟》SideStory 活动复刻开启，限定干员卡池同步开放
```

LLM 只输出：

```json
{
  "core_queries": [
    "明日方舟 活动复刻",
    "明日方舟 限定卡池",
    "明日方舟 SideStory 复刻"
  ],
  "platform_queries": {
    "bilibili": ["明日方舟 活动复刻", "明日方舟 限定卡池"],
    "weibo": ["明日方舟复刻", "明日方舟限定卡池"],
    "tieba": ["明日方舟 sidestory 复刻"]
  }
}
```

即使它生成得不好，你也可以保留原始 query 兜底。

---

## 2000 条资讯够不够？

够做第一版。

2000 条适合做：

```text
重复新闻判断 baseline
事件聚类 baseline
搜索召回测试
query expansion 对比
同游戏/同事件评测集
```

但不一定够做“很强的 RAG”。
没关系，你现在目标不是大规模知识库，而是先验证：

```text
我能不能从历史资讯中找出重复、旧闻、新进展？
```

这个 2000 条足够开始。

---

## 你需要提前保存哪些参数？

为了以后复现和 LangSmith 对齐，建议每次 run 存这些：

```json
{
  "run_id": "20260615_xxx",
  "mode": "baseline",
  "platforms": ["bilibili", "weibo", "tieba"],
  "time_window_hours": 48,
  "query_expansion_version": "v0.1",
  "dedup_version": "v0.1",
  "rerank_version": "v0.1",
  "same_event_threshold": 0.75,
  "duplicate_threshold": 0.9,
  "freshness_threshold": 0.6,
  "simhash_threshold": 3,
  "embedding_model": null,
  "llm_model": null,
  "vector_index": null
}
```

等以后接 LLM，就变成：

```json
{
  "mode": "shadow",
  "llm_model": "xxx",
  "prompt_version": "same_event_judge_v1",
  "temperature": 0,
  "structured_output_schema": "EventJudgementV1"
}
```

这样你能回答面试官一个很关键的问题：

> 你怎么证明 LLM 接入后真的提升了，而不是只是变复杂了？

---

## 我建议你的近期实施顺序

你可以按这个来做：

```text
第 1 步：建 SQL schema
news_items / games / events / event_news_links / search_runs / decision_traces

第 2 步：导入 2000 条资讯
保留原始字段，不要过度清洗覆盖原文。

第 3 步：做 title_norm / url_norm / hash
先实现完全重复和近似重复判断。

第 4 步：做 game alias 匹配
把资讯和游戏实体连起来。

第 5 步：按时间顺序构建 event
新资讯进来时，先查最近 N 天同游戏事件，再判断是否合并。

第 6 步：记录每一步 trace
不用 LangSmith，先本地 JSONL / SQL 记录。

第 7 步：搜索智能化 shadow mode
LLM 只生成 query，不影响主判断。

第 8 步：引入向量库
event summary / title / content embedding，作为相似事件召回补充。

第 9 步：LLM 灰区判断
只处理规则不确定样本。

第 10 步：再考虑 LangGraph / LangSmith
当流程出现分支、循环、人工确认、失败恢复时，LangGraph 的状态持久化和 checkpoint 会更有用。LangGraph 官方的 persistence 是把图状态保存为 checkpoint，用于 human-in-the-loop、memory、time travel debugging 和容错执行。:contentReference[oaicite:2]{index=2}
```

---

## 结论

你的方案不是“慢”，而是**正确的工程地基**。

我会把你的路线改写成一句项目定位：

> 先构建基于关系数据库的游戏资讯事件库，实现确定性去重、事件聚合、历史追踪与可复现 trace；随后引入向量检索和 RAG 增强相似事件召回，并从搜索 query 智能化开始逐步接入 LLM，最终形成可观测、可评估、可回滚的游戏资讯 Agent 工作流。

这条路线很适合写进简历，因为它体现的是：

```text
数据建模
信息检索
去重聚类
RAG 扩展
LLM 安全接入
Agent workflow
工程可观测性
```

不是单纯“套了一个 Agent 框架”。

[1]: https://docs.langchain.com/langsmith/observability?utm_source=chatgpt.com "LangSmith Observability - Docs by LangChain"
[2]: https://docs.langchain.com/oss/python/langchain/observability?utm_source=chatgpt.com "LangSmith Observability - Docs by LangChain"
